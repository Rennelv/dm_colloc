#ifndef MUL_PP_P_HPP
#define MUL_PP_P_HPP

#include "P/Polynomial.hpp"

/*
    Выполнил Щеглов Михаил, группа 3381

    Функция MUL_PP_P выполняет умножение двух многочленов.
    Принимает:
        p1 - первый многочлен
        p2 - второй многочлен
    Возвращает:
        Polynomial - результат умножения многочленов p1 и p2
*/

Polynomial MUL_PP_P(const Polynomial& p1, const Polynomial& p2);

#endif
